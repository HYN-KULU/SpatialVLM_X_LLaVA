from llava.model.builder import load_pretrained_model
from llava.mm_utils import get_model_name_from_path
from llava.eval.run_llava import eval_model

model_path = "liuhaotian/llava-v1.5-7b"

tokenizer, model, image_processor, context_len = load_pretrained_model(
    model_path=model_path,
    model_base=None,
    model_name=get_model_name_from_path(model_path)
)

# prompt = "Can you describe the wooden block under the metal box? How many blocks are in the image? Please comment on the image?"
prompt = "First analyze whether the apple is red or blue, and then say True or False, is the apple in the picture blue?"
image_file = "../datasets/f3rm/panda/scene_001/images/frame_00029.jpg"
# print("Model Name: ",get_model_name_from_path(model_path))
args = type('Args', (), {
    "model_path": model_path,
    "model_base": None,
    "model_name": get_model_name_from_path(model_path),
    "query": prompt,
    "conv_mode": None,
    "image_file": image_file,
    "sep": ",",
    "temperature": 0,
    "top_p": None,
    "num_beams": 1,
    "max_new_tokens": 512
})()

eval_model(args)